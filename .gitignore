# Secrets — never commit
.env

# Generated, self-contained data stores (the notebook recreates these on run).
# Anchored to the repo root with a leading slash so the curated samples/ copies
# (samples/figures, samples/reports) are NOT ignored.
/news_db/
/forecaster_db/
/checkpoints_db/
/metrics_db/
/vector_db/
/figures/
/reports/

# Python / Jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
